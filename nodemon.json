{
  "watch": ["src", ".env"],
  "ext": "js,ts,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts"],
  "exec": "npm run build && ts-node -r tsconfig-paths/register --transpile-only src/server.ts"

}